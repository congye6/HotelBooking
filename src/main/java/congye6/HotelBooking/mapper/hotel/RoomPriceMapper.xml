<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="congye6.HotelBooking.mapper.hotel.RoomPriceMapper">
	<select id="getPrice" resultType="double">
		select `price` from room where hotelId=#{id} and `type`=#{type}
	</select>
</mapper>